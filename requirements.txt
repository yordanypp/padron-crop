# Runtime core (CPU-only, pip-installable everywhere).
#   pip install -r requirements.txt
numpy>=1.24
Pillow>=10.0
opencv-python-headless>=4.8

# Optional extras (install only what you need):
#   pip install -e ".[sql]"    -> SQLAlchemy             (direct DB ingestion)
#   pip install -e ".[api]"    -> httpx                  (API ingestion)
#   pip install -e ".[ocr]"    -> pytesseract            (+ Tesseract binary) OCR anchoring
#   pip install -e ".[huge]"   -> pyvips                 (hundreds-of-MP images, low RAM)
#   pip install -e ".[dev]"    -> pytest
